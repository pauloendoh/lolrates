import { SkillLevelTypes } from "../draft/SkillLevelTypes";
import { ChampionRoleType } from "../../LolRate/ChampionRoleType";

export interface ChampionDto {
  id: number;

  name: string;

  iconUrl: string;

  createdAt: string;

  updatedAt: string;


}
