import styled from "styled-components";

const S = {
  TextEditorWrapper: styled.div({
    width: "50%",
    border: "1px solid grey",
    padding: 8,
    borderRadius: 4,

    "& .MUIRichTextEditor-placeHolder-26": {
      position: "relative",
    },
  }),
};

export default S;
