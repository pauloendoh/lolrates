import FlexCenter from "@/components/_common/flexboxes/FlexCenter";
import styled from "styled-components";

const S = {
  PageRoot: styled(FlexCenter)(({ theme }) => ({
    height: "100%",
    width: "100%",
  })),
};

export default S;
