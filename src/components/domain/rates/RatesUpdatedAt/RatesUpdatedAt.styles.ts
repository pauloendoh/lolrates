import FlexCol from "@/components/UI/Flexboxes/FlexCol";
import { spacing } from "@/utils/theme";
import styled from "styled-components";

export default {
  Root: styled(FlexCol)({
    marginTop: spacing(2),
  }),
};
