import { Box, styled } from "@material-ui/core";

export default {
  Root: styled(Box)({
    width: 375,
    height: 300,
  }),
  FloatingLabelsWrapper: styled(Box)({
    position: 'relative'
  })
};
