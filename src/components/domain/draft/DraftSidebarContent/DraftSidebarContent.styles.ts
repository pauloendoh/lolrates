import FlexVCenter from "@/components/UI/Flexboxes/FlexVCenter";
import styled from "styled-components";

export default {
  ListItemContent: styled(FlexVCenter)({
    width: "100%",
    justifyContent: "space-between",
  }),
};
